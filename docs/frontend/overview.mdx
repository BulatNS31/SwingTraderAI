---
title: "Frontend Overview"
description: "Кратко о frontend и его связях с backend API"
---

# Frontend Overview

- Папка `frontend/` содержит SPA (Vite).
- SPA использует API `/api/v1/*` (FastAPI).
- UI отвечает за: аутентификацию, dashboard, watchlist, отображение сигналов.

## TODO

- Указать точный стек (React / Vue / Svelte) — проверить содержимое `frontend/src/`.
