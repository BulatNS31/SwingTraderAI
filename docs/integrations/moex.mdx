---
title: "MOEX Integration"
description: "Интеграция с MOEX (подтверждённые элементы)"
---

# MOEX

- README упоминает MOEX через yfinance (`SBER.ME`, `GAZP.ME` и т.д.) и moexalgo.
- В `pyproject.toml` присутствуют `yfinance` и `moexalgo`.

## Рекомендации

- Убедиться, что тикеры корректно нормализуются (`SBER.ME` и т.д.) и timezone (`Europe/Moscow`) корректно обработаны.

## TODO

- Добавить код-линки к адаптерам в `swingtraderai/services` (если есть).
